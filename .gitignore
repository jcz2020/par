_opam/
_build/
.merlin
*.install

# ----------------------------------------------------------------
# Internal development docs (HIDDEN from public repo, see git history)
# ----------------------------------------------------------------
# Strategic intent and design rationale belong in a private repo.
# These are gitignored but still in git history (recoverable via
# `git checkout <commit> -- <path>`).
docs/STRATEGY.md
docs/v0.[0-9]-ROADMAP.md
docs/v0.[0-9].[0-9]-ROADMAP.md
docs/plans/
docs/rules/
docs/release.md
DESIGN.md

AGENTS.md

# Agent / planning / issue-tracker state (sisyphus, opencode, beads)
.sisyphus/
.opencode/
.beads/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Python binding build artifacts (par_runtime ctypes)
bindings/python/build/
bindings/python/dist/
bindings/python/*.egg-info/
bindings/python/par_runtime/*.so
__pycache__/
*.pyc
*.pyo
