# P-A-R v0.3.0 开发路线图

**版本**: v0.3.0
**状态**: 🚧 战略讨论已完成 ✅ — 进入实施规划阶段
**目标读者**: LLM 后端工程师（部署性 > 学术性）
**前置版本**: v0.2.0（198 tests passing）
**参考**: [pi-agent-core](https://github.com/earendil-works/pi/tree/main/packages/agent) — 58.9k stars，验证过的 agent core 设计

---

## ⚠️ 关键边界声明

**PAR 是 SDK 库，不是部署框架。**

- ✅ SDK 提供**机制**（mechanism）：暴露内部状态查询接口（`Runtime.health()`、`Runtime.metrics()`）
- ❌ SDK **不**提供**策略**（policy）：不假设用户用什么部署环境（k8s / systemd / bare metal / 嵌入式 wrapper）
- ❌ SDK **不**内置 HTTP server
- ❌ SDK **不**绑定任何部署形态

用户怎么暴露 health 端点、怎么部署、怎么监控——是用户的责任。PAR 只负责提供查询函数。

OPS-1 系列（原提案"部署工程"）的修正：把"添加 health 端点"改成"添加 `Runtime.health()` 函数"。**机制与策略分离**。

---

## ✅ P0 战略定义（已完成）

### 1. 价值定位
**PAR = OCaml 圈的 LangChain + LangGraph**。
- MVP 友好：问答、RAG、翻译工具
- 严肃场景：8 状态机、Human-in-the-Loop、多 agent 协作
- 集成组件 + 提示词管理

### 2. 目标用户
**LLM 后端工程师**——agent 是产品一部分，重视：
- 部署性（opam + PyPI 双发布）
- 运行时稳定性、故障恢复
- 类型严谨（build-time error catching）

### 3. 差异化主张
vs LangChain (Python):
- **类型严谨**：LangChain 能崩的错，PAR 在 build 时就报
- **Eio 轻量**：同负载下 PAR 内存/CPU 仅为 Python 的 1/5
- **原生多 provider**：OpenAI / Anthropic / Ollama / Custom 零成本切换

vs pi-agent-core (TypeScript):
- **OCaml 生态唯一选择**：他们不重叠，PAR 是 OCaml 圈唯一选项

### 4. 优先级（资源分配）
1. **类型严谨**（最高）
2. **并发性能**（Eio 优势）
3. **多 provider 适配**（变现点）
4. **OCaml 生态定位**（人口天花板）

### 5. 发布策略
**opam + PyPI 双发布**：
- `par` — OCaml SDK（opam）
- `par_runtime` — Python 包（PyPI，通过 ctypes）

---

## 概述

v0.2.0 已完成 198 个测试。v0.3.0 聚焦**三件事**：

1. **补 v0.2 留下的债**——3 个真实 bug（retry middleware 死代码、silent persistence error、cancel_task 跨 FFI 崩溃）
2. **修 v0.2 暴露的 SEVERE 类型 gap**——5 个用户实际遇到的 runtime 崩溃点
3. **借鉴 pi-agent-core 提升交互价值**——steering、onUpdate、parallel tool

**来源标注**：
- **🔴 P0 债**：v0.2 实施时已知但未修的 bug
- **🟡 UX-5**：v0.2 后类型审计发现的 SEVERE gap（Q3 #1 主张"build-time error catching"目前**部分**不成立）
- **🟡 UX-1/2/3**：来自 [pi-agent-core](https://github.com/earendil-works/pi)（58.9k stars，验证过的设计模式）
- **🟡 UX-4**：v0.2 review 时发现 system_prompt 是裸 string 的功能缺口
- **🟢 FFI-1/2**：v0.2 stub 化遗留
- **🟡 OPS-1a/b**：Q2 目标用户（LLM 后端工程师）需要内部可观测性 hook

### 完整 v0.3.0 项目清单（按 Wave 分组）

| Wave | ID | 名称 | 估时 | 来源 |
|------|------|------|------|------|
| **W1** | OPS-2 | 修复 retry middleware 死代码 | 半天 | 🔴 P0 债 |
| **W1** | OPS-3 | 取消 silent persistence error masking | 半天 | 🔴 P0 债 |
| **W1** | OPS-4 | cancel_task raise → Result | 15 分钟 | 🔴 P0 债 |
| **W1** | UX-4 | System Prompt 设计 | 1 天 | 🟡 缺口 |
| **W1** | FFI-1 | 完整 Python SDK | 3 小时 | 🟢 债 |
| **W2** | UX-5a | Tool schema 验证（input_schema 真校验） | 1-2 天 | 🟡 SEVERE #1 |
| **W2** | UX-5b | 重复 tool name → Result 错误 | 半天 | 🟡 SEVERE #3 |
| **W2** | UX-5c | 范围验证（api_key/provider/buffer） | 1 天 | 🟡 SEVERE #4/#5 + MODERATE |
| **W2** | UX-5d | valid_agent smart constructor | 1 天 | 🟡 SEVERE 收口 |
| **W2** | FFI-2 | Python 集成测试 | 1 小时 | 🟢 债 |
| **W2** | UX-1 | Steering/Follow-up 队列 | 半天 | 🟡 pi 学 |
| **W3** | OPS-1a | Runtime.health() 函数（FFI + Python） | 半天 | 🟡 Q2 必需 |
| **W3** | OPS-1b | JSON metrics 导出（无 Prometheus 依赖） | 1 天 | 🟡 Q2 必需 |
| **W3** | UX-2 | onUpdate 工具流式 | 1 天 | 🟡 pi 学 |
| **W3** | UX-3 | Parallel tool execution | 2 天 | 🟡 pi 学 |

**总估时**: ~12-14 天

### 关键洞察：3 个 P0 债必须在 v0.3 启动

`OPS-2` / `OPS-3` / `OPS-4` 不是"v0.3 加新功能"，是"v0.2 留下没修的 bug"：

- **OPS-2**：`retry.ml:94-96` 明确标注 `on_error` 是死代码。**当前 LLM 429 错误不会重试**——所有 429 直接报错返回。
- **OPS-3**：`runtime.ml` 有 7+ 处 `Error _ -> ()` 静默忽略 persistence 错误。**数据丢失用户无感知**。
- **OPS-4**：`runtime.ml:107` `cancel_task` 抛 `Invalid_argument` 异常。**Python 端调用会崩溃整个进程**（跨 FFI 边界异常未捕获）。

如果不做这 3 个修复，所有 v0.3 新功能（UX-1/2/3/4/FFI-1）都建立在已知 bug 之上。

---

## FFI-1: 完整 Python SDK

**问题**: `par_capi.ml` 表面有 7 个函数，5 个是 stub 返回 0 或抛 `NotImplementedError`。Python 用户无法真正使用 `register_agent` + `invoke` 跑通 hello world。

**当前状态** (`lib/ffi/par_capi.ml`):
- `par_init` ✅ 真实现
- `par_register_tool` ✅ 真实现
- `par_shutdown` ✅ 真实现
- `par_register_agent` ❌ stub
- `par_invoke` ❌ stub
- `par_submit_workflow` ❌ stub
- `par_approve_workflow` ❌ stub
- `par_resume_workflow` ❌ stub

**文件**:
- `lib/ffi/par_capi.ml` (FFI stub 化)
- `lib/ffi/par_ffi.c` (C ABI 入口)
- `lib/ffi/par_ffi.h` (头文件)
- `bindings/python/par_runtime/runtime.py` (`NotImplementedError`)

**修复方案**: 实现 5 个 stub 函数的真路径。OCaml 端调用 `Par.Runtime.register_agent`、`Par.Runtime.invoke`、`Par.Runtime.submit_workflow`、`Par.Runtime.approve_workflow`、`Par.Runtime.resume_workflow`。Python 端 `runtime.py` 删除 `NotImplementedError`，添加内存管理（C `malloc` 字符串 + Python 端 `par_free` 释放）。

**成功标准**:
- 5 个 stub 函数全部实现真路径
- Python 端 5 行代码跑通：register_tool → register_agent → invoke → 收到响应 → close
- 5 个集成测试覆盖正常路径 + 错误路径（duplicate id、invalid JSON、invoke after close）
- C 端字符串所有权清晰（malloc → par_free）

**估时**: 3 小时

---

## FFI-2: Python 集成测试

**问题**: 现有 Python 测试 (`bindings/python/tests/test_runtime.py`) 仅 5 个，FFI 真实现后无端到端验证。

**文件**: `bindings/python/tests/test_runtime.py`

**修复方案**: 新增 `test_par_sdk.py`，包含 10 个集成测试：
1. `test_register_and_invoke_basic` — 完整 happy path
2. `test_register_agent_invalid_json` — 非法 JSON 抛 `PARInitError`
3. `test_register_agent_duplicate_id` — 重复 id 抛 `PARInitError`
4. `test_invoke_unknown_agent` — invoke 不存在 agent 抛 `PARInvokeError`
5. `test_invoke_after_close` — close 后 invoke 抛 `PARError`
6. `test_tool_handler_invoked` — 注册的 tool handler 真的被调用
7. `test_tool_handler_returns_value` — tool 返回值传到 LLM
8. `test_workflow_submit_creates_run` — submit workflow 拿到 run_id
9. `test_workflow_approve_resume` — approve → resume 工作流推进
10. `test_concurrent_runtimes` — 多个 Runtime 实例隔离

**成功标准**:
- 10 个测试全部通过
- pytest 覆盖率 100% 覆盖 `runtime.py` 公共 API
- 集成测试在 CI 中跑通

**估时**: 1 小时

---

## UX-1: Steering / Follow-up 队列

**来源**: [pi-agent-core `steer()` / `followUp()`](https://github.com/earendil-works/pi/blob/main/packages/agent/README.md#steering-and-follow-up)

**问题**: PAR 的 `Runtime.invoke(agent_id, message)` 是单次调用返回。长跑 agent（30s+ LLM 推理 + 工具链）期间，用户无法：
- **Steer** — 中途注入新指令（如 "Stop! Do this instead"）
- **Follow-up** — 在 agent 跑完后挂后续任务（如 "Also summarize the result"）

pi-agent-core 在 TypeScript 生态验证了这个模式有效（58.9k stars，多项目使用）。

**移植设计**（适配 PAR 的 Eio 同步纤程范式）:

```ocaml
(* Runtime API 新增 *)
type steering_queue
type followup_queue

val create_steering_queue : unit -> steering_queue
val create_followup_queue : unit -> followup_queue

(* 在 invoke 期间被 Runtime 内部轮询 *)
val poll_steering : steering_queue -> (message list, error_category) result
val poll_followup : followup_queue -> (message list, error_category) result
```

**集成点** (`lib/core/engine.ml`):
- ReAct 循环每轮 tool 执行完后检查 steering 队列
- 队列非空 → 注入到对话 → 继续 LLM 调用
- 队列空 + tool 跑完 → 检查 followup 队列
- followup 非空 → 注入新消息 → 继续
- 都空 → 退出循环

**成功标准**:
- 单元测试：steer 队列非空时 ReAct 循环立即拉取并注入
- 单元测试：followup 队列在 loop 出口被检查
- 集成测试：Python 端调用 `Runtime.steer()` 真的中断正在跑的 invoke
- 集成测试：Python 端调用 `Runtime.follow_up()` 在 invoke 返回后立即触发下一轮
- 现有 198 tests 不退化

**估时**: 半天

**风险**: Eio 同步纤程与队列的并发模型需要仔细设计。建议参考 pi-agent-core 的 `PendingMessageQueue` 实现但适配 OCaml。

---

## UX-2: onUpdate 工具流式输出

**来源**: [pi-agent-core `AgentTool.execute` 的 `onUpdate` 回调](https://github.com/earendil-works/pi/blob/main/packages/agent/README.md#tools)

**问题**: PAR 的 13 个内置工具都是 `string -> string` 同步返回。对于长跑工具（fetch_url 抓 10MB 页面、read_webpage 解析大型 HTML、generate_password 长参数），用户看到的是"卡住 → 突然返回"。

pi-agent-core 的解决方案：tool handler 接收 `onUpdate` 回调，可以在执行过程中回传部分结果。

**移植设计**（最小侵入）:

```ocaml
(* tool_descriptor 新增字段 *)
type tool_descriptor = {
  ...
  on_update : (string -> unit) option;  (* 工具执行进度回调 *)
}

(* Handler 类型保持不变，但支持可选更新 *)
type handler_fn = tool_call -> cancellation_token -> (handler_result * (string -> unit) option)
```

**集成点**:
- `lib/providers/openai_provider.ml` 已经有 SSE 流式响应 → 流式 LLM 输出已支持
- `lib/core/engine.ml` 的 `execute_tool` 函数包装 handler 调用
- 当 handler 暴露 `on_update` 回调，engine 将其注册到 event bus，事件类型新增 `Tool_progress` variant

**成功标准**:
- 单元测试：tool handler 调用 `on_update("reading...")` 触发 event bus 事件
- 集成测试：`fetch_url` 工具在抓取大页面时每秒发一个 progress 事件
- Python 端：`Runtime.subscribe(event_type="tool_progress", callback=...)` 能收到
- 现有 198 tests 不退化

**估时**: 1 天

**不做的部分**:
- LLM 流式输出已经在 openai_provider.ml 有 SSE 实现，Python 端暂不暴露
- 不重写现有工具 handler（保持向后兼容）

---

## UX-3: Parallel tool execution

**来源**: [pi-agent-core `toolExecution: "parallel"` 配置](https://github.com/earendil-works/pi/blob/main/packages/agent/README.md#with-tool-calls)

**问题**: PAR 的 ReAct 循环在一次 LLM 响应中收到多个 `tool_call` 时，串行执行。LLM 经常一次吐 2-3 个 tool_call（典型场景：先 search 再 fetch）。串行执行浪费时间。

pi-agent-core 的解决方案：`executeToolCallsParallel` —— preflight（参数校验 + before_tool hook）串行做，但 tool handler 执行并发。

**移植设计**（需要重构 engine.ml）:

```ocaml
(* 新增 tool 调度器 *)
type tool_batch = {
  preflight : (tool_call * (tool_call -> tool_call option)) list;  (* 顺序：参数校验 + before_tool *)
  execution : tool_call list;  (* 并发：handler 调用 *)
  finalize : (tool_call * handler_result -> handler_result option) list;  (* 顺序：after_tool hook *)
}

val execute_tool_batch : Eio.Switch.t -> tool_batch -> (handler_result list, error_category) result
```

**集成点** (`lib/core/engine.ml`):
- ReAct 循环收齐 LLM 返回的所有 tool_call
- 第一阶段：串行 preflight（调用每个 tool 的 before_tool hook，校验参数）
- 第二阶段：用 `Eio.Fiber.all` 并发执行所有 tool handler
- 第三阶段：串行 finalize（调用 after_tool hook，按 LLM 返回顺序发射 tool_result）

**成功标准**:
- 单元测试：2 个 tool_call 并发执行，总时间 ≈ max(t1, t2) 而非 t1+t2
- 单元测试：preflight 失败时整个 batch 不执行
- 单元测试：finalize 按 LLM 源顺序发射 tool_result（不是完成顺序）
- 集成测试：3 个 fetch_url 并发执行总时间 < 串行 1/3
- 现有 198 tests 不退化

**估时**: 2 天

**风险**:
- Eio 纤程的取消语义需要重新设计（一个 tool 失败时其他 tool 的命运）
- 现有 `middleware` 的 `on_before_tool` / `on_after_tool` 钩子必须保留语义

**可配置**:
- 全局：`Runtime.config.parallel_tool_execution : bool`（默认 true）
- 单 tool：`tool_descriptor.execution_mode : [ `Sequential | `Parallel ]`（默认 Parallel）
- 串行优先：若 batch 中任一 tool 标 `Sequential`，整个 batch 降级为串行（学 pi）

---

## UX-4: System Prompt 设计 ✅ 方案敲定

### 选定方案：A + 行业最佳实践组合

**问题**: PAR 的 `agent_config.system_prompt` 是裸 `string`，所有 6 个文档示例都是 `"You are a helpful assistant."` 静态文本。

**选定方案（3 决策已完成）**:

1. **API 形态**：方案 A（向后兼容）
   - 新增 `system_prompt_template : system_prompt_template option` 字段
   - 保留 `system_prompt : string`（v0.2 既有）作为 Static 简化形式
   - **不破坏** v0.2 用户代码

2. **模板语法**：Mustache `{{var}}`
   - 跨语言友好（Python Jinja2、Go template、JS Handlebars 同源）
   - 学习成本低，LLM 后端工程师已熟悉
   - 语法简单（不引入控制流，降低 XSS 风险）

3. **注入变量**：系统 + tool 描述
   - `{{current_time}}` — UTC ISO 8601
   - `{{agent_id}}` — 当前 agent 标识
   - `{{runtime_id}}` — runtime 标识
   - `{{user_variables}}` — 用户提供的 key-value（Map）
   - `{{available_tools}}` — 已注册工具的 JSON 列表

4. **Provider 适配**：单一字符串，PAR 透传
   - 用户写一个 `system_prompt_template`
   - PAR 渲染为字符串
   - PAR 内部根据 provider 转换：
     - **OpenAI**: 塞入 `messages[0].role = "system"`
     - **Anthropic**: 塞入顶层 `system` 字段
     - **Ollama**: 塞入 `messages[0].role = "system"`
     - **Custom**: 用户自己决定
   - **不暴露** system vs developer role 区别（学 LangChain / pi-agent-core / Vercel AI SDK）

### API 形态

```ocaml
(* lib/core/types.ml 新增 *)
type system_prompt_template = {
  template : string;          (* Mustache 模板 *)
  variables : string list;    (* 允许的变量名白名单（type safety） *)
  required : string list;      (* 必填变量（缺则报错） *)
}
[@@deriving yojson]

(* agent_config 新增字段（v0.2 既有 system_prompt 保持不变） *)
type agent_config = {
  id : string;
  system_prompt : string;                          (* 静态文本，v0.2 兼容 *)
  system_prompt_template : system_prompt_template option;  (* v0.3 新增，optional *)
  model : model_config;
  ...
}
```

### 渲染流程

```
user template + variables dict
     │
     ▼
┌────────────────────────────────────────┐
│ Mustache.render(template, vars)       │
│   - 替换 {{var}}                        │
│   - 验证 vars ⊆ whitelist (类型严谨)   │
│   - 必填检查 (required 都存在)         │
└────────────────────────────────────────┘
     │
     ▼
Rendered string
     │
     ▼
┌────────────────────────────────────────┐
│ provider adapter (按 model.provider)  │
│   - Openai  → messages[0].system      │
│   - Anthropic → top-level system       │
│   - Ollama  → messages[0].system       │
│   - Custom  → custom_fn user defined   │
└────────────────────────────────────────┘
```

### 行业对照（验证方案选择）

| 框架 | 暴露给用户 | 自动转换 | 适配模式 |
|------|----------|---------|---------|
| **LangChain** | 单一字符串 | ✅ | 同 PAR |
| **pi-agent-core** | 单一字符串 | ✅ | 同 PAR |
| **Vercel AI SDK** | 独立 `system` 属性 | ✅ | 同 PAR |
| **LiteLLM** | messages 数组 | ✅ | 略不同 |

**3/4 主流框架选择"用户写单一字符串 + 框架自动转"**。PAR 方案契合产业标准。

### 成功标准

- [ ] `system_prompt_template` 字段被 agent_config 支持
- [ ] Mustache 模板引擎实现（轻量内嵌，不引外部依赖）
- [ ] 5 个内置变量（current_time, agent_id, runtime_id, user_variables, available_tools）实现
- [ ] 4 个 provider 适配器全部实现 system_prompt 透传
- [ ] 单元测试：10+ 个用例覆盖渲染、变量校验、必填检查
- [ ] 集成测试：1 个完整场景（注册有 system_prompt_template 的 agent，invoke 后看到渲染结果）
- [ ] Python FFI 端：register_agent 支持 system_prompt_template 字段
- [ ] 文档更新：quickstart.md 添加模板示例

**估时**: 1 天

---

## 🔴 P0 债：v0.2 留下的 3 个真实 bug

**这 3 个不是新功能，是 v0.2 实施时已知但未修的债。必须先修，否则 v0.3 所有新功能都建立在 bug 之上。**

### OPS-2: 修复 retry middleware 死代码

**问题**: `lib/middleware/retry.ml:94-96` 明确标注：

```ocaml
(* CONCURRENCY NOTE: attempt ref is shared across concurrent fibers sharing this middleware instance.
   on_error is currently dead code (engine never calls apply_on_error). When wired up, on_error
   must accept conversation parameter for per-request state isolation. *)
```

**实际影响**: 当前 LLM 429 错误**不会重试**——所有 429 直接报错返回。Retry middleware 形同虚设。

**修复**:
- `lib/core/engine.ml:120` 当前是 `Result.Error err -> Result.Error err`
- 改为：调用 `apply_on_error agent.middleware err conv` 并尊重 retry result
- `apply_on_error` 签名需扩展：加 `conversation` 参数（per-request state isolation）
- 添加 retry 触发测试：mock LLM 第一次返回 429，第二次返回成功——验证最终收到 success

**成功标准**:
- mock LLM 第一次 429，第二次成功，调用方收到 `Ok result`
- 现有 198 tests 不退化
- 添加测试：`test_retry_429_succeeds_on_second_attempt`

**估时**: 半天

---

### OPS-3: 取消 silent persistence error masking

**问题**: `lib/core/runtime.ml` 有 7+ 处静默忽略 persistence 错误：

```ocaml
(match rt.services.persistence.save_task_state_fn task with
 | Ok () -> () | Error _ -> ());  (* SILENTLY IGNORED *)
```

出现在 line 87-88, 112, 123, 157, 175, 181, 196, 203。

**实际影响**: 数据丢失用户**完全无感知**。LLM 后端工程师（Q2 用户）会看到"事件应该保存"但实际没保存——无法 debug。

**修复**:
- 所有 `Error _ -> ()` 改为 `Error e -> Logs.err (fun m -> m "Persistence save failed: %a" pp_error e)`
- `lib/persistence/sqlite_persistence.ml` 和 `lib/postgres/postgres_persistence.ml` 同步检查
- 考虑：增加 `Persistence_error` event 暴露到 event bus（下游可订阅告警）

**成功标准**:
- 7+ 处静默忽略全部改为 `Logs.err`
- 添加测试：`test_persistence_failure_logs_error`（mock persistence 失败，验证 error log）
- 不破坏现有 198 tests

**估时**: 半天

---

### OPS-4: cancel_task raise → Result

**问题**: `lib/core/runtime.ml:107`：

```ocaml
| Ok None | Error _ -> raise (Invalid_argument "Task not found")
```

**实际影响**: **跨 FFI 边界崩溃 Python 进程**。Python `par_runtime.cancel_task()` 调用会引发未捕获异常，进程 crash。同模块 `invoke`（line 58）和 `approve_task`（line 117）已用 `Result.Error (Invalid_input ...)`——`cancel_task` 是漏网之鱼。

**修复**:
- `runtime.ml:107` 改为 `Result.Error (Invalid_input "Task not found")`
- 添加测试：`test_cancel_unknown_task_returns_error`
- Python FFI 端 `cancel_task` wrapper 不需要 catch exception（已会处理 Result）

**成功标准**:
- 单行修复
- 添加 1 个测试覆盖错误路径

**估时**: 15 分钟

---

## 🟡 OPS-6: Tool call hook 机制（用户决策 2026-06-03）

**用户洞察**（2026-06-03）: "虽然 PAR 是 SDK，用户交互式确认我们可以做支持啊，在 Agent 执行工具和数据流上面做文章"

**完全正确**。PAR 是 SDK，但**SDK 可以提供"机制"让用户自己实现"策略"**——这与之前"机制与策略分离"边界声明一致。

### pi-agent-core 调研结论（librarian `bg_c590e316`）

pi 提供 28+ 事件 hook，**`tool_call` 是最关键**：

```typescript
// pi 源码
type ToolCallEventResult = {
  block?: boolean;       // 阻止工具执行
  reason?: string;       // 阻止原因
  // in-place mutate event.input 修改工具参数
};

pi.on("tool_call", async (event, ctx) => {
  if (ctx.hasUI) {                       // 有 UI（TUI/RPC 模式）
    const choice = await ctx.ui.select(   // 异步询问用户
      `⚠️ Dangerous command:\n  ${event.input.command}\n\nAllow?`,
      ["Yes", "No"]
    );
    if (choice !== "Yes") {
      return { block: true, reason: "Blocked by user" };
    }
  } else {                                // 无 UI（batch/JSON 模式）
    return { block: true, reason: "Dangerous command (no UI)" };
  }
});
```

**关键设计原则**:
- **无 handler 装入 = no-op**（不强制注册）
- **同一 hook 代码工作于 CLI / TUI / Web / IDE**（只换 `ctx.ui` 实现）
- **Async handler**（可以等用户点击）
- **Modify input in-place**（修改参数后再执行）
- **First-block-wins**（多个 hook 链）

### PAR OPS-6 设计

```ocaml
(* lib/core/hook.ml *)

type tool_call_decision =
  | Allow
  | Block of { reason : string }
  | Modify of { input : Json.t }     (* in-place 风格 *)

type tool_call_context = {
  tool_name : string;
  tool_call_id : string;
  input : Json.t;
  has_ui : bool;                       (* 用户的 wrapper 决定 *)
  runtime_alive : bool;
}

(* 用户的 hook 实现 *)
type user_confirmation_hook = 
  tool_call_context -> tool_call_decision

(* PAR Runtime 暴露 *)
val register_tool_call_hook : t -> user_confirmation_hook -> unit
```

### 关键设计决策

1. **不绑 UI**（与之前边界声明一致）
   - PAR 不实现 dialog、modal、popup
   - 用户在 wrapper 层实现
   - 无 UI 时 hook 默认不装入

2. **多 hook 链**（学 pi）
   - 多个 hook 可注册，按注册顺序执行
   - 第一个 `Block` 阻止后续
   - `Modify` 后继续（下一个 hook 看到修改后的 input）

3. **失败默认**（**比 pi 更安全**）
   - hook 抛异常 → PAR **默认 block**（fail-closed）
   - pi 行为不明确，PAR 显式 fail-closed
   - 这是安全姿态 vs pi 的对比优势

4. **可选性**（学 pi）
   - 无 hook 装入 = 工具直接执行
   - 用户按需注册
   - 不强制任何应用层 UI 集成

### 4 种用户场景（PAR 机制 vs 用户策略）

| 用户类型 | user_confirmation_hook 实现 |
|---------|------------------------------|
| **CLI 用户**（par_cli） | terminal read_line + yes/no |
| **Web 服务用户** | HTTP endpoint 返回 `{confirm: bool}` |
| **IDE 插件** | LSP call 到 IDE |
| **Batch 脚本** | 不注册 hook（永远 allow） |
| **高安全用户** | 注册 hook + 黑名单正则（"rm -rf" 自动 block） |
| **多用户审批** | hook 内部调企业微信 / Slack API 异步等待 |

### 与 v0.3.0 其他项目的协同

- **TOOL-1g bash**：`bash_blacklist` 黑名单是**编译时**的——不通过 hook
  - 黑名单 = 硬性拒绝（无用户干预）
  - hook = 用户可选的运行时检查
  - **两者互补**：黑名单是最后防线，hook 是常规审批

- **UX-1 steering/follow-up**：hook 是"工具级确认"，steering 是"agent 级中断"
  - 两者正交：可以同时存在
  - hook 在 tool_call 触发，steering 在 turn_end 触发

### 估时

| 子任务 | 时间 |
|--------|------|
| `lib/core/hook.ml`（类型 + 决策类型 + ctx record） | 0.25 天 |
| `Runtime.register_tool_call_hook` 字段 + 注册逻辑 | 0.25 天 |
| 引擎集成（execute_tool 前调用 hook） | 0.5 天 |
| 多 hook 链实现（顺序 + first-block-wins） | 0.25 天 |
| 测试（5+ 用例：allow / block / modify / 异常 / 多 hook） | 0.25 天 |
| 文档 + 示例 | 0.25 天 |
| **总估时** | **1.75 天** |

### Wave 安排

**Wave 3**（与 UX-2/3 + TOOL-1e/f 并行）:
- 用户决策是 v0.3.0 后期需求（不是债）
- 但 v0.3.0 不做就推 v0.4——届时工具集都齐了再补 hook 反而要回头改
- 早期集成能指导 TOOL-1g bash 的安全设计

**优先级**: 🟡 中（不是债，但 v0.3.0 不做 = 推迟到 v0.4+）

---

## 🟡 UX-5 (收敛版): 修 5 个 SEVERE 类型 gap

**原提案**（"1-2 周 GADT/phantom type 重构"）**过度设计**。Oracle 评审指出：现有 ADT 已足够严谨，phantom type 是学术。**修正**：只修 explore agent 审计出的 **5 个 SEVERE gap**（用户实际会撞到的运行时崩溃）。

### 5 个 SEVERE gap（用户实际会撞到）

| # | Gap | 文件:行 | 后果 |
|---|-----|---------|------|
| 1 | Tool handler 接收 untyped Yojson.Safe.t | `types.ml:182`, `engine.ml:67` | LLM 幻觉发 `{"count": "five"}`，handler 期望 int → 运行时 crash |
| 2 | cancel_task raise | `runtime.ml:107` | （即 OPS-4，重复列入） |
| 3 | 重复 tool name 静默覆盖 | `tool_registry.ml:13` | 第二次 register 静默覆盖第一次，开发者无感知 |
| 4 | model.provider ↔ llm_providers key 不匹配 | `types.ml` runtime_config | provider 设置为 `Openai` 但 llm_providers 给了 `anthropic` key → "LLM not initialized" 运行时错 |
| 5 | OpenAI api_key 空串不验证 | `par_config.ml:24` | 第一次 LLM 调用才报 auth 失败，不在 config 时报 |

### UX-5a: Tool schema 验证

**问题**: `tool_descriptor.input_schema : Yojson.Safe.t` 是无类型 blob。LLM 产生错误类型时 handler 崩溃。

**修复**:
- 引入轻量 `json_schema` 验证（不引外部依赖，手写 mini validator）
- 验证 LLM 实际 tool_call.arguments 是否符合 `input_schema` 定义（JSON Schema subset: type, required, properties）
- 失败时返回 `handler_result.Error { category = Invalid_input; message = "Schema mismatch" }` 而不是抛异常
- engine.ml:67 `execute_tool` 加 schema validation 调用

**成功标准**:
- tool handler 收到类型不符的 input 时，**不崩溃**，返回结构化 Error
- 添加测试：`test_tool_schema_mismatch_returns_error`

**估时**: 1-2 天

---

### UX-5b: 重复 tool name → Result 错误

**问题**: `tool_registry.ml:13` `Hashtbl.replace` 静默覆盖。

**修复**:
- `register` 返回 `(unit, [ `Duplicate_tool of string ]) result` 而不是 `unit`
- 添加 pre-check：`Hashtbl.mem tbl name` 存在则返回 `Duplicate_tool`
- `Runtime.register_tool` 把内层 error 透传到外层

**成功标准**:
- 重复注册同名 tool 返回 `Error Duplicate_tool`
- 添加测试：`test_register_duplicate_tool_returns_error`

**估时**: 半天

---

### UX-5c: 范围验证（api_key / provider / buffer / temperature）

**问题**: 多个字段接受无效值：
- `api_key = ""` —— 第一次 LLM 调用才报 auth 错
- `model.provider` variant 与 `llm_providers` key 不匹配 —— 运行时"LLM not initialized"
- `event_bus_config.buffer_capacity = 0` —— 事件总线死锁
- `model_config.temperature = -1.0 / 999.0` —— LLM API 拒绝
- `resource_quota.max_concurrent_tasks = 0` —— 信号量死锁

**修复**:
- 集中到一个 `lib/core/validation.ml` 模块
- 暴露 `validate_runtime_config : runtime_config -> (unit, error_category) result`
- `Runtime.create` 在创建前调用验证
- 修复点：
  - api_key 非空（provider 是 Openai/Anthropic 时）
  - llm_providers 必须包含 model.provider 对应的 entry
  - buffer_capacity > 0
  - temperature ∈ [0.0, 2.0]
  - max_concurrent_tasks > 0

**成功标准**:
- 5+ 个测试覆盖各种无效 config → 返回明确 Error
- 不破坏现有 198 tests

**估时**: 1 天

---

### UX-5d: valid_agent smart constructor

**问题**: `agent_config` 是个 record，**直接构造可绕过所有验证**。v0.2 没有任何"造出来一定是 valid 的"保证。

**修复**:
- `lib/core/agent_config.ml`（或 types.ml 内）
- `make_agent : id:string -> system_prompt:string -> model:model_config -> tools:tool_descriptor list -> max_iterations:int -> middleware:middleware_hook list -> (agent_config, error_category) result`
- 内部调用所有 UX-5a/b/c 验证
- `valid_agent` 类型：构造后是不可变 record（无 setter 接口）

**成功标准**:
- `Runtime.register_agent` 使用 `valid_agent` 而非裸 record
- 添加测试：构造无效 agent 返回 `Error`，构造有效 agent 返回 `Ok valid_agent`

**估时**: 1 天

---

## 🟡 OPS-1 (收敛版): Runtime 内部可观测性（Q2 用户的 hook 需求）

**重要边界修正**：

**PAR 是 SDK 库，不是部署框架**。SDK 的责任是：
- 提供**机制**（mechanism）：暴露 `health()` 函数让用户查询内部状态
- **不**提供**策略**（policy）：不假设用户用什么部署环境（k8s / systemd / Docker / bare metal）

**用户怎么用这些机制**——是用户的事：
- k8s 用户：自己写个 HTTP wrapper 调 `par_runtime.health()` 暴露 `/health` 端点
- systemd 用户：写个 `par_runtime.health()` 包装 script 给 `Type=notify` 用
- CLI 用户：根本不用
- 嵌入式 Web 服务用户：在自己的 Dream/Aristo handler 里调

**SDK 假设部署 = 越界**。本次 OPS-1 仅暴露内部状态查询接口，不绑定任何部署形态。

**原提案**（"3-4 天"包含 health + metrics + shutdown + retry budget）**过宽**。Oracle + Explore 评审：
- health function KEEP（去掉"端点"——SDK 不暴露 HTTP 端点）
- metrics KEEP（纯 key-value 返回，不引 Prometheus 库）
- graceful shutdown DROP（不影响正确性）
- retry budget 部分被 OPS-2 覆盖

### OPS-1a: Runtime 内部 health() 函数

**问题**: 无内部状态查询函数。用户无法判断 runtime 是否健康、persistence 是否可用、最近 LLM 调用是否成功。

**边界**:
- PAR **不**暴露 HTTP 端点（不假设部署环境）
- PAR **不**内置任何 HTTP server
- PAR **只**提供 `Runtime.health() -> health_status record` 函数
- 用户自己在自己的 wrapper 里决定怎么用这个函数

**修复**:
- `lib/core/runtime.ml` 新增 `val health : t -> (health_status, error_category) result`
- `health_status` record:
  - `runtime_alive : bool` — runtime 已初始化、未被 close
  - `last_llm_call_at : float option` — 最近 LLM 调用时间戳
  - `last_llm_call_status : [ `Success | `Error of error_category | `Never_called ]`
  - `persistence_ok : bool` — 上次持久化操作成功
- FFI 透传：`par_health() -> int`（0=ok, 1=closed, 2=error）
- Python：`Runtime.health() -> dict`

**成功标准**:
- 4 个 health 字段全部实现
- 3 个测试：`test_health_when_open`、`test_health_after_close`、`test_health_persistence_failure`
- 不假设任何部署形态
- 估时: 半天

---

### OPS-1b: Runtime metrics() 函数（同 OPS-1a 边界）

**问题**: 无 invoke 计数、错误计数、延迟分布。用户无法监控 runtime。

**修复**:
- `Runtime.metrics() -> (string * int) list` 返回 key-value 计数器
- 计数器:
  - `llm_requests_total` (按状态码 200/401/429/500 标签，或单 counter + 多个状态方法)
  - `task_completed_total` / `task_failed_total`
  - `tool_invocations_total` (按 tool name 标签)
  - `events_published_total` / `events_dropped_total`
- **不引 Prometheus 库**——纯 Python `dict` 返回，让 Python 侧集成到现有监控系统
- 实现：event bus 在 publish 时增加 counter

**成功标准**:
- 5+ 个计数器
- 不增加外部依赖
- 添加测试：`test_metrics_counts_invocations`

**估时**: 1 天

---

## 🟡 TOOL-1: 内置 pi 7 个 coding 工具（用户拍板 2026-06-03）

**用户决策**（2026-06-03）：**全部 7 个工具内置**到 PAR。

虽然 pi-agent-core SDK 不带工具（`coding-agent` 包才有），但 PAR 用户**明确希望 SDK 自身就具备 coding 能力**，避免依赖外部 coding-agent。理由：
- PAR 战略 Q1 = "LangChain + LangGraph 等价物"——LangChain 自己是带工具的
- 实战中 LLM 后端工程师（Q2 用户）会要"AI 写代码/改文件/查代码"
- **"走 MCP" 方案**虽然优雅但**增加用户集成成本**——能内置就内置

### 7 个工具的子项目拆分

| 子项 | 名称 | 风险 | 估时 |
|------|------|------|------|
| **TOOL-1a** | `read` — 本地文件读（含图片、offset/limit 分页） | 低 | 1 天 |
| **TOOL-1b** | `ls` — 目录列表 | 低 | 0.5 天 |
| **TOOL-1c** | `find` — glob 文件搜索（`.gitignore` 感知） | 低 | 0.5 天 |
| **TOOL-1d** | `grep` — 内容搜索（regex/literal，glob 过滤） | 低 | 0.5 天 |
| **TOOL-1e** | `write` — 文件写（自动创建父目录） | 中（误覆盖风险） | 0.5 天 |
| **TOOL-1f** | `edit` — 精准替换（支持批量 + 自动合并重叠） | 中 | 1 天 |
| **TOOL-1g** | `bash` — Shell 执行（CWD 锁定 + 超时） | 🔴 **高** | 1-2 天 |

**总估时**: 5-6 天

### 实施分组（按风险 + 依赖）

**Phase 1: 仅读类（低风险，无依赖）—— 2.5 天**
- TOOL-1a read
- TOOL-1b ls
- TOOL-1c find
- TOOL-1d grep

**Phase 2: 可写类（中风险，需要安全设计）—— 1.5 天**
- TOOL-1e write
- TOOL-1f edit

**Phase 3: 可执行类（高风险，必须安全设计）—— 1-2 天**
- TOOL-1g bash

### TOOL-1g bash 安全设计（升级版：采用"Agent-Bash-Runtime"方案的 ADT + Functor 设计模式）

LLM 调用 bash 是**最危险的内置工具**。基于：
- pi-agent-core 调研（`bg_a2e1bbb9`，2026-06-03）
- 用户提供的"Agent-Bash-Runtime"方案设计模式（2026-06-03）
- 战略锚点："PAR 是 SDK，不引 bwrap/landlock 沙箱"（避免跨平台锁定）

**核心升级**：从"裸 shell string + 黑名单"升级为"**类型化 Safe_command ADT + Policy Functor + 黑名单**"——把安全检查从运行时前移到**编译期**，体现 Q4 #1 "类型严谨"。

#### 设计模式来源（4 个借鉴）

| 设计模式 | 来源 | PAR 价值 |
|---------|------|---------|
| **`Safe_command` ADT**（无 `Exec_raw_shell` 构造器）| "Agent-Bash-Runtime" 方案 §3.1 | 🟢 编译期拦截注入 |
| **Policy Functor**（`module type POLICY`）| "Agent-Bash-Runtime" 方案 §3.3 | 🟢 编译期确定安全边界 |
| **9 维度安全机制**（CWD/黑名单/白名单/超时/进程组/环境脱敏/截断/ANSI 剥离/审计）| pi + 该方案合并 | 🟢 比 pi 默认强 |
| **风险评分 + 结构化审计**（事件 bus 暴露）| 该方案 §3.7 | 🟢 完善 v0.2 缺失 |

#### Safe_command ADT（v0.3.1 实施）

```ocaml
(* lib/tools/bash_safe_command.ml *)
(* 没有 Exec_raw_shell 构造器——从类型上杜绝 shell 注入 *)

type sandboxed_path = private Path of string
val sandboxed_path_of_string : string -> (sandboxed_path, error_category) result
(** 构造时校验：拒绝 `..`、绝对敏感路径（/etc、~/.ssh 等） *)

type command =
  | Exec of {                                  (* 不接受裸 shell string！*)
      argv : string list;                       (* 必须是 argv 数组 *)
      cwd : sandboxed_path;
      env : (string * string) list;             (* 已脱敏的 env *)
      timeout : float;                          (* 秒 *)
    }
  | Pipeline of command list                    (* 已知安全命令的有限管道 *)
  | No_op                                      (* 用于测试/禁用 *)

(* 风险评分：编译期已知，运行时快速评估 *)
type risk = Low | Medium | High | Critical
val assess_risk : command -> risk
```

**关键安全约束**：
- **没有 `Exec_raw_shell`**——所有 shell 命令必须分解为 argv 数组
- 路径通过抽象类型 `sandboxed_path` 强制 CWD 相对性
- 管道仅允许连接已知安全的 `command list`
- 风险评分在 ADT 构造后即可计算（无需看运行时行为）

#### Policy Functor（v0.3.1 实施）

```ocaml
(* lib/tools/bash_policy.ml *)
module type POLICY = sig
  val name : string
  val filter : command -> (command, error_category) result
  val max_cpu_seconds : float
  val max_memory_kb : int
  val allow_network : bool
  val allow_write : bool
end

(* 预置策略——可被 Runtime.config.bash_policy 覆盖 *)
module ReadOnly : POLICY = ...        (* 任何写操作拒绝 *)
module ReadOnlyNoNet : POLICY = ...   (* + 网络拒绝 *)
module Coder : POLICY = ...           (* 默认：可写 + 可联网 + 60s timeout *)
```

**关键设计**：
- **Policy 在 `Runtime.create` 时注入**（不运行时切换）
- 不同 Agent 可绑定不同 Policy（编译期即确定）
- Policy Functor 保证**无策略 = 无执行**（不能 "忘了配"）

#### 9 维度安全机制（采纳）

| # | 机制 | 状态 | 设计 |
|---|------|------|------|
| 1 | **CWD 锁定** | ✅ 硬性不可关 | `sandboxed_path` 抽象 + 构造时校验 |
| 2 | **黑名单** | ✅ 默认 + 可关 | 黑名单在 Policy Functor 的 `filter` 实现里 |
| 3 | **白名单模式** | 🟡 可选配置 | Policy Functor 自定义实现 |
| 4 | **超时强制** | ✅ 有默认 60s | `Eio.Process.spawn` + `killpg` |
| 5 | **进程组清理** | ✅ 硬性 | `setpgid` + `killpg -SIGKILL` |
| 6 | **环境脱敏** | ✅ 有默认（**比 pi 强**） | 默认 strip `*_SECRET*` / `*_KEY*` 等 |
| 7 | **输出截断** | ✅ 有（学 pi） | 50KB byte / 2000 lines |
| 8 | **二进制清洗** | ✅ 有（学 pi） | ANSI escape 剥离 |
| 9 | **审计日志** | ✅ 走 event bus | `Bash_invoked` / `Bash_completed` 事件 |

#### 暂不实施（v0.4+ 候选）

| 机制 | 理由 | 引入版本 |
|------|------|---------|
| **OS 沙箱**（bwrap / sandbox-exec） | 跨平台锁定、需要 root 权限、**单独立项 `par_sandbox` opam 包** | v0.4+ 评估 |
| **用户交互式确认** | OPS-6 hook 机制已提供 | v0.4+ 通过 hook 集成 |
| **Landlock / seccomp** | Linux 专属、要求现代内核 | v0.5+ 评估 |

#### 与 pi 对比（PAR 优势）

| 维度 | pi 默认 | **PAR v0.3.1** |
|------|--------|-----------|
| 类型化命令 | ❌ 裸 shell string | ✅ **Safe_command ADT** |
| 策略边界 | ❌ Extension 模式 | ✅ **Policy Functor**（编译期） |
| 敏感信息脱敏 | ❌ | ✅ 默认 strip |
| 黑名单 | ❌ | ✅ 默认有 |
| 超时默认值 | ❌ | ✅ 60s |
| 进程组清理 | ✅ | ✅ |
| CWD 锁定 | ✅ | ✅（**用 ADT 强化**） |
| 截断 / ANSI 剥离 | ✅ | ✅ |

#### 关键实施细节

- **`lib/tools/bash_safe_command.ml`**：ADT + 路径校验 + 风险评分
- **`lib/tools/bash_policy.ml`**：Policy Functor + 3 个预置策略
- **`lib/tools/bash_blacklist.ml`**：~30 个黑名单正则（在 Policy 的 `filter` 中使用）
- **集成**: `builtin_tools.ml` 暴露 `bash_policy` 字段
- **CWD 锁定**: `sandboxed_path_of_string` 构造时拒绝 `..` 和 `/etc`、`~/.ssh` 等
- **进程组清理**: `Eio.Process.spawn` 配合 `setpgid`，`killpg` 杀整个组
- **审计**: emit `Bash_invoked { command; pid; cwd; timestamp; risk }` 在 spawn 前

#### 估时

| 子任务 | 时间 |
|--------|------|
| 设计 + 写出 `bash_safe_command.ml`（ADT + 路径校验 + 风险评分） | 0.5-0.75 天 |
| 设计 + 写出 `bash_policy.ml`（Policy Functor + 3 个预置策略） | 0.5 天 |
| `bash_blacklist.ml`（~30 个正则） | 0.25 天 |
| 集成到 `builtin_tools.ml`（bash 工具实现） | 0.5 天 |
| 测试（10+ 用例：ADT 构造、Policy 过滤、blacklist、happy path、timeout、env 脱敏） | 0.5 天 |
| **总估时** | **2.25-2.5 天** |

### pi 工具的设计模式（PAR 复用）

参考 pi 实现的几个关键模式：

1. **Truncation 统一** — 共享 truncation 子系统（byte + line 限制），所有工具 truncation 行为一致
2. **多 edit 批量** — `edit` 工具支持 `edits[]` 数组，自动合并重叠
3. **分类渲染** — `read` 工具对特殊文件（`AGENTS.md`、`SKILL.md`、docs）做不同渲染
4. **`.gitignore` 感知** — `find` 和 `grep` 默认跳过 `.gitignore` 中的路径
5. **可插拔操作接口** — pi 的工具接受 operations 接口（默认本地 FS，可换 SSH 远程），PAR 暂不实现但留好扩展点

### 实施要求（7 个工具通用）

- 加在 `lib/tools/builtin_tools.ml`
- 工具实现要求：使用 `Cancellation.with_timeout`，符合 Eio 协作式调度
- 必走 `test_builtin_tools.ml` 测试套件
- 文档同步到 `docs/sdk/tools.md`（如有）
- **OCR 风险评估**（v0.3.0 启动前必做）:
  - `read`/`ls`/`find`/`grep` — 无写权限，**低风险**
  - `write`/`edit` — 需 CWD 限制，**中风险**
  - `bash` — 需 CWD 锁定 + 黑名单 + 超时，**高风险**

### 估时汇总

| 子项 | 估时 | 优先级 |
|------|------|--------|
| TOOL-1a read | 1 天 | 🟡 高 |
| TOOL-1b ls | 0.5 天 | 🟡 高 |
| TOOL-1c find | 0.5 天 | 🟡 高 |
| TOOL-1d grep | 0.5 天 | 🟡 高 |
| TOOL-1e write | 0.5 天 | 🟡 中 |
| TOOL-1f edit | 1 天 | 🟡 中 |
| TOOL-1g bash | 1-2 天 | 🟡 高（因风险） |
| **总估时** | **5-6 天** | |

### Wave 安排建议

TOOL-1 拆 3 Phase：
- **Wave 2 末尾**（与 UX-1 并行）: Phase 1（read/ls/find/grep）—— 2.5 天
- **Wave 3 末尾**（与 UX-2/3 并行）: Phase 2（write/edit）—— 1.5 天
- **v0.3.0 之后**（**单独 v0.3.1**）: Phase 3（bash）—— 1-2 天，**因为安全设计需要单独 review**

**理由**: bash 工具的安全设计涉及 LLM 后端工程师的实际使用风险（Q2 用户场景），值得**单独立项**让用户单独 review，避免"和 7 个工具一起 commit"模糊注意力。

---

## 🟡 MCP-1: MCP (Model Context Protocol) 客户端（v0.3.1 候选）

**状态**: 🚧 **预留位置**

**问题**: 2025 后 LLM 工具生态的事实标准是 [MCP](https://modelcontextprotocol.io/)。LangChain/LlamaIndex 都在接 MCP。PAR 当前没有 MCP 客户端——意味着 PAR 用户无法直接用 1000+ 现有 MCP server。

**战略锚点**: 符合"SDK 做机制不做策略"原则——PAR 不必自带所有工具，提供**接任意 MCP server**的机制即可。

**实施计划**（待 v0.3.0 实施前细化）:
- `lib/mcp/mcp_client.ml` — MCP stdio 协议客户端（JSON-RPC over stdio）
- `Runtime.register_mcp_server` API
- MCP 工具作为 `tool_descriptor` 暴露给 PAR
- 工具调用走 Eio 纤程

**依赖**:
- 不引外部 MCP 库（MCP 协议简单，自己实现 ~500 行）
- 进程间 JSON-RPC 用 yojson（已有依赖）

**估时**: 3-5 天（v0.3.1 估时，未列入 v0.3.0 核心）

---

## 估时汇总

按 Wave 分组，每 Wave 估时：

### Wave 1（紧急债 + UX-4 + FFI-1）—— ~3.5 天

| ID | 名称 | 估时 | 优先级 |
|------|------|------|--------|
| OPS-2 | 修复 retry middleware 死代码 | 半天 | 🔴 P0 债 |
| OPS-3 | 取消 silent persistence error | 半天 | 🔴 P0 债 |
| OPS-4 | cancel_task raise → Result | 15 分钟 | 🔴 P0 债 |
| UX-4 | System Prompt 设计 | 1 天 | 🟡 高 |
| FFI-1 | 完整 Python SDK | 3 小时 | 🟢 必做 |

### Wave 2（UX-5 + FFI-2 + UX-1 + TOOL-1 Phase 1）—— ~9 天

| ID | 名称 | 估时 | 优先级 |
|------|------|------|--------|
| UX-5a | Tool schema 验证 | 1-2 天 | 🟡 SEVERE #1 |
| UX-5b | 重复 tool name → Result | 半天 | 🟡 SEVERE #3 |
| UX-5c | 范围验证（api_key/provider/buffer） | 1 天 | 🟡 SEVERE #4/#5 + MODERATE |
| UX-5d | valid_agent smart constructor | 1 天 | 🟡 SEVERE 收口 |
| FFI-2 | Python 集成测试 | 1 小时 | 🟢 必做 |
| UX-1 | Steering/Follow-up 队列 | 半天 | 🟡 高（pi 学） |
| **TOOL-1a** | **read（本地文件读）** | **1 天** | 🟡 高 |
| **TOOL-1b** | **ls（目录列表）** | **0.5 天** | 🟡 高 |
| **TOOL-1c** | **find（文件 glob）** | **0.5 天** | 🟡 高 |
| **TOOL-1d** | **grep（内容搜索）** | **0.5 天** | 🟡 高 |

### Wave 3（OPS-1 + UX-2/3 + TOOL-1 Phase 2 + OPS-6）—— ~8 天

| ID | 名称 | 估时 | 优先级 |
|------|------|------|--------|
| OPS-1a | Runtime.health() 函数 | 半天 | 🟡 Q2 必需 |
| OPS-1b | JSON metrics 导出 | 1 天 | 🟡 Q2 必需 |
| UX-2 | onUpdate 工具流式 | 1 天 | 🟡 中（pi 学） |
| UX-3 | Parallel tool execution | 2 天 | 🟡 中（pi 学） |
| **TOOL-1e** | **write（文件写）** | **0.5 天** | 🟡 中 |
| **TOOL-1f** | **edit（精准替换）** | **1 天** | 🟡 中 |
| **OPS-6** | **Tool call hook 机制** | **1.75 天** | 🟡 中（用户决策 2026-06-03） |

**v0.3.0 总计**: ~21.25 天（约 4 周）

> 含 OPS-6 hook 机制（用户 2026-06-03 决策加入）

### v0.3.1 候选（启动 v0.3.0 前由用户决策）

| ID | 名称 | 估时 | 备注 |
|------|------|------|------|
| **TOOL-1g** | **bash（Shell 执行）** | 1-2 天 | 🔴 高风险，单独立项让用户 review 安全设计 |
| **MCP-1** | **MCP 客户端** | 3-5 天 | 仍按原计划 deferred |

---

## 📋 PAR Known Tech Debt（v0.2 留下的小修小补）

**用户洞察**（2026-06-03）："OPS-2/3/4 这种'0.5+'小修小补也得记下来，将来可以做。"

**背景**: v0.3 Wave 1 的 OPS-2/3/4 是**已知 v0.2 bug**，0.5+0.5+15 分钟是它们的估时。但工程实践中，类似的小修小补**很容易被忽略**——直到下次有人撞上同样的 bug。

**目的**: 本章节追踪**已知的、估时小、可推迟的"小债"**，防止它们永远藏在代码注释里。

### 已知 Tech Debt 清单

| ID | 描述 | 估时 | 触发场景 | 来源 |
|----|------|------|---------|------|
| **OPS-2** | 修复 retry middleware 死代码（`on_error` 未被 engine 调用） | 0.5 天 | LLM 429 错误不重试，现状 | `retry.ml:94-96` 注释 |
| **OPS-3** | 取消 silent persistence error masking（7+ 处 `Error _ -> ()`） | 0.5 天 | 数据丢失用户无感知 | `runtime.ml` 多处 |
| **OPS-4** | `cancel_task` raise → Result | 15 分钟 | 跨 FFI 边界崩溃 Python | `runtime.ml:107` |
| **OPS-7** | `on_error` hook 真死代码（与 OPS-2 同源，已含） | (含 OPS-2) | 同 OPS-2 | 同 OPS-2 |
| **OPS-8** | `Event_bus.ml` 死信队列不可观测（DLQ 仅内存 list） | 0.25 天 | 部署后丢失事件无告警 | explore 审计 |
| **OPS-9** | `Eio.Stream.add` 满载时阻塞（无 backpressure 拒绝） | 0.25 天 | 高负载下挂起 | explore 审计 |
| **OPS-10** | Engine `execute_tool` 无 schema 验证（LLM 幻觉发 `{"count": "five"}` 崩溃） | 0.5 天 | LLM 工具调用类型错 | `engine.ml:67` 审计 |
| **OPS-11** | `temperature` 无范围验证（接受 -1.0 / 999.0） | 0.25 天 | LLM API 拒绝 | explore 审计 |
| **OPS-12** | `max_concurrent_tasks = 0` 接受（信号量死锁） | 0.25 天 | 信号量死锁 | explore 审计 |
| **OPS-13** | `event_bus_config.buffer_capacity = 0` 接受（事件总线死锁） | 0.25 天 | 事件总线死锁 | explore 审计 |
| **OPS-14** | `Openai` `api_key = ""` 不验证（运行时第一次 LLM 调用才报） | 0.25 天 | 启动→第一次调用才报错 | explore 审计 |
| **OPS-15** | `Runtime.metrics` 全局计数器无（无 invoke 计数、错误计数） | 1 天 | LLM 后端工程师无监控 | explore 审计 |
| **OPS-16** | `par_postgres` 子库代码重复（与 sqlite_persistence 共享接口但独立实现） | 0.5 天 | 维护成本 ×2 | 代码审视 |
| **OPS-17** | `engine.ml` 错误信息吞掉（`apply_on_error` 链路静默） | 0.25 天 | debug 困难 | `engine.ml` 审计 |

**总债务估时**: 约 **5-6 天**（分散实施）

### 何时清理

| 触发条件 | 行动 |
|---------|------|
| **撞上 bug** | 立即修（不推迟） |
| **改相关模块** | 顺手修（review 强制） |
| **季度清理** | 每月挑 1-2 项做掉（防债积累） |
| **特定 commit 触发** | 该 commit 的相关 OPS 强制清（例如 v0.3.1 触发 OPS-10/11/12/13/14） |

### 关联性

- OPS-2 + OPS-7 同源（retry middleware 死代码）
- OPS-10/11/12/13/14 全部在 `Runtime.create` 路径上——**UX-5c 范围验证是根治方案**
- OPS-8/9 在 event_bus 模块——任何 event bus 重构会触发
- OPS-16 在 v0.3 阶段 par_postgres 移出主库时已部分解决

### 文档约定

- 每条 OPS-X 都应该在 `git commit` message 里标注（如 `fix(ops-2): wire up retry on_error`）
- 每条 OPS-X 完成后，从本清单中删除（**完成后即消失**——避免过时债清单）

---

## 🟢 v0.4 候选（启动 v0.3.0 后由用户决策）

### OPS-PROC-1: Persistent Process Manager（持久进程管理器）

**来源**: "Agent-Bash-Runtime" 方案 §3.6

**问题**: LLM 在 dev workflow 场景下需要启动 dev server / watch 模式进程。当前 PAR 工具都是 fire-and-forget（read/grep/ls 立即返回）。LLM 想"启动 React dev server 然后看日志"——**当前做不到**。

**典型场景**:
- 启动 Vite/Webpack dev server
- 跑 `npm run dev` / `cargo run` / `python -m flask run`
- 启动数据库（测试用 SQLite、PostgreSQL）
- 跑 long-running 测试

**设计**（基于该方案 §3.6）:

```ocaml
(* lib/core/process_manager.ml *)

type process_id = string

type spawn_spec = {
  argv : string list;
  cwd : sandboxed_path;
  env : (string * string) list;
  max_lifetime : float;            (* 秒 *)
  max_output_buffer_lines : int;   (* 默认 2000 *)
}

type process_status =
  | Running
  | Exited of { exit_code : int; duration : float }
  | Killed of { reason : string }

type t
val create : max_total:int -> t
val spawn : t -> spawn_spec -> (process_id, error) result
val read_output : t -> process_id -> offset:int -> max_lines:int 
                 -> (string * int, error) result
  (** 返回 (新行内容, 新 offset) —— 避免重复传输 *)
val stop : t -> process_id -> signal:[`TERM | `KILL] -> (unit, error) result
val list : t -> (process_id * process_status) list
val cleanup : t -> (unit, error) result  (* 关闭所有进程 *)
```

**关键安全设计**（与 TOOL-1g bash 复用）:
- 共享 `sandboxed_path` 抽象（CWD 锁定）
- 共享 `command` ADT（不能传入裸 shell string）
- 共享 Policy Functor（不同 Agent 限制不同）
- 共享超时（`max_lifetime` 默认 300s，硬性）
- 共享进程组清理（`killpg` 防 fork 逃逸）
- **审计**: emit `Process_spawned` / `Process_exited` 事件

**核心集成**:
- `lib/core/hook.ml`（OPS-6 已有）支持 `process_spawn` 事件——用户可在 hook 里加"启动 dev server 前必须 confirm"
- `Runtime.config` 加 `max_concurrent_persistent_processes : int`
- 自动清理：`Runtime.close` 时自动 `cleanup` 所有进程

**工具集成**:
- TOOL-1h: `start_process` (argv + cwd + env)
- TOOL-1i: `read_process_output` (pid + offset)
- TOOL-1j: `stop_process` (pid)
- TOOL-1k: `list_processes` (返回 pid 列表)

**估时**: 3-5 天

**与 TOOL-1g bash 的关系**:
- bash 工具实现可以**直接用 Process Manager** 作为后端（瞬态命令 = spawn + wait + cleanup）
- 避免重复实现进程管理代码
- Persistent Process Manager 是 bash runtime 的**超集**

**依赖**:
- TOOL-1g bash（共享 ADT、Policy、超时机制）
- OPS-6 hook（用户确认）

### OPS-SANDBOX-1: Sandbox Layer（OS 沙箱，可选依赖）

**问题**: 当前所有 TOOL-1g bash 安全都靠**应用层机制**（黑名单、env 脱敏、超时）。**真正的攻击面**只能由 OS 层隔离（bwrap / landlock / sandbox-exec）解决。

**设计**（基于该方案 §3.2）:
- 单独立项 `par_sandbox` opam 包
- v0.4 阶段把 bwrap / landlock wrapper 实现
- v0.4+ 阶段把 macOS sandbox-exec 加上
- 仍然 2 包结构（par 仍 2 个包：par + par_cli，par_sandbox 是独立 opam 包）

**原则**: PAR **不强制** sandbox。用户选择：
- 不装 `par_sandbox` → 用应用层安全（v0.3.1 的 ADT + Policy）
- 装 `par_sandbox` → TOOL-1g 自动用 OS 沙箱（透明）

**估时**: 2-3 周（需要内核 + bwrap 知识）

**风险**:
- 跨平台不一致
- 需要 root 或 user namespace 配置
- 自己实现 wrapper 容易出 escape bug

**决策点**: 启动 v0.3.0 后，由用户决定 v0.4 是不是要做这个。如果做——单独立项，确保 PAR 核心包**仍然轻量**。

### MCP-1: MCP (Model Context Protocol) 客户端

（**保留**——v0.3.1 候选）
- 用户可决定 v0.4 是否升级到正式实施
- v0.4 估时 3-5 天
- 仍按原计划 deferred

---

## 不做的项目（防呆清单）

### v0.3.0 明确拒绝

| 项目 | 不做的理由 | 评审来源 |
|------|----------|---------|
| 完整 GADT/phantom type 重构 | 学术化，无用户可见价值 | Oracle |
| 完整 benchmark README 表 | 数字 stale 快 | Oracle |
| Graceful shutdown 实现 | 部署期痛，不影响正确性 | Explore |
| Circuit breaker | YAGNI for v0.3 | Explore |
| DLQ observability | 内部数据，缺用户视角 | Explore |
| Event bus backpressure | `Eio.Stream.add` 阻塞可管理 | Explore |
| Invocation count 全局计数器 | 没人用 | Explore |
| Prometheus 依赖 | 纯 JSON 即可 | Oracle + Explore |
| Dream HTTP 集成示例 | PAR 是库不是 server | Oracle |
| dune 模板 | dune init 已够用 | Oracle |
| Session tree + branching | PAR 用 workflow + PG 持久化 | 历史决策 |
| Compaction（LLM 摘要压缩） | 走显式 `context_strategy` | 历史决策 |
| Thinking levels | Provider-specific | 历史决策 |
| Custom message types | TS 专属 | 历史决策 |
| Skills system (SKILL.md) | 产品决策 | 历史决策 |

---

## 依赖关系

```
FFI-1 (基础) ──→ FFI-2 (依赖 FFI-1 真实现)
UX-1 (独立) ──→ 可与 FFI 并行
UX-2 (依赖 event bus 扩展) ──→ 可与 UX-1 并行
UX-3 (依赖 engine.ml 重构) ──→ 需独立串行（避免合并冲突）
UX-4 (独立) ──→ 可与所有其他并行
UX-5a/b/c/d (SEVERE gap 链) ──→ 串行（a → b → c → d）
TOOL-1a/b/c/d (仅读类) ──→ 可与 UX-1 并行
TOOL-1e/f (可写类) ──→ Wave 3 末尾
TOOL-1g (bash) ──→ v0.3.1 单独 review（🔴 高风险）
```

**建议执行顺序**（按 Q4 优先级 + bug 严重性 + 风险排序）:

**Wave 1（必先做——债 + UX-4 + FFI-1）**:
- 串行：**OPS-2 → OPS-3 → OPS-4**（3 个 P0 债，15-30 分钟一个，必须先修）
- 并行：**UX-4**（System Prompt 模板）+ **FFI-1**（Python SDK stub 真实现）
- 估时: ~3.5 天

**Wave 2（UX-5 收敛 + FFI-2 + UX-1 + TOOL-1 Phase 1）**:
- 串行：**UX-5a → UX-5b → UX-5c → UX-5d**（SEVERE gap 修复链）
- 依赖 FFI-1：**FFI-2**（Python 集成测试）
- 并行：**UX-1**（steering 队列）+ **TOOL-1a/b/c/d**（仅读类工具 4 个）
- 估时: ~9 天

**Wave 3（OPS-1 + UX-2/3 + TOOL-1 Phase 2）**:
- 并行：**OPS-1a**（health）、**OPS-1b**（metrics）、**UX-2**（onUpdate）、**UX-3**（parallel tool）、**TOOL-1e**（write）、**TOOL-1f**（edit）
- 估时: ~6 天

**v0.3.0 总估时**: ~19.5 天（约 4 周）

**v0.3.1 候选**:
- **TOOL-1g bash**（1-2 天，**单独立项让用户 review 安全设计**）
- **MCP-1** MCP 客户端（3-5 天）

**原则**:
- **bug 修复永远在 feature 之前**（OPS-2/3/4 → 一切）
- **类型严谨** (Q4 #1) 体现在 UX-5 系列（修 SEVERE gap），不在学术重构
- **Q2 LLM 后端工程师** 实际痛点（health/metrics）放最后——新功能先稳定
- **风险分层**: TOOL-1 拆 3 Phase（仅读 → 可写 → 可执行），bash 单独立项
